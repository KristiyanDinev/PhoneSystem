version: '2'

services:
  db:
    image: 'postgres'
    volumes:
      - 'data:/var/lib/postgresql/data'
    ports:
      - '5432:5432'
    environment:
      - 'POSTGRES_USER=AdminPostgresqlUser123'
      - 'POSTGRES_PASSWORD=VTUYiuhoNonnio'

volumes:
  data: